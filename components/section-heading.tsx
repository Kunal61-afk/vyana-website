export function SectionHeading({ eyebrow, title, copy, align = "left" }: { eyebrow: string; title: string; copy?: string; align?: "left" | "center" }) { return <div className={align === "center" ? "mx-auto max-w-2xl text-center" : "max-w-2xl"}><p className="eyebrow">{eyebrow}</p><h2 className="heading text-3xl leading-tight sm:text-4xl">{title}</h2>{copy && <p className="mt-5 text-lg leading-8 text-slate-600">{copy}</p>}</div>; }
