"use client";
import { ArrowUp, MessageCircle } from "lucide-react"; import { useEffect, useState } from "react";
export function FloatingActions() { const [visible, setVisible] = useState(false); useEffect(() => { const listener = () => setVisible(window.scrollY > 500); window.addEventListener("scroll", listener); return () => window.removeEventListener("scroll", listener); }, []); return <div className="fixed bottom-5 right-5 z-30 flex flex-col gap-3"><a href="https://wa.me/919876543210" target="_blank" aria-label="Chat on WhatsApp" className="rounded-full bg-[#25D366] p-3.5 text-white shadow-lg transition hover:scale-105"><MessageCircle size={22}/></a>{visible && <button onClick={() => window.scrollTo({ top: 0, behavior: "smooth" })} aria-label="Scroll to top" className="rounded-full bg-ink p-3.5 text-white shadow-lg transition hover:scale-105"><ArrowUp size={20}/></button>}</div>; }
