"use client";
import Link from "next/link"; import { Menu, X } from "lucide-react"; import { useState } from "react"; import { navigation } from "@/lib/content";
export function Header() { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-40 border-b border-slate-200/70 bg-mist/90 backdrop-blur"><div className="container-page flex h-[76px] items-center justify-between"><Link href="/" className="font-display text-xl font-bold tracking-tight text-ink">Vyana <span className="text-brand">Digital</span></Link><nav className="hidden items-center gap-7 lg:flex" aria-label="Primary navigation">{navigation.map((item) => <Link className="text-sm font-medium text-slate-600 transition hover:text-brand" key={item.href} href={item.href}>{item.name}</Link>)}</nav><Link href="/contact" className="hidden rounded-full bg-brand px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-blue-600/20 transition hover:bg-blue-700 lg:block">Get Free Consultation</Link><button onClick={() => setOpen(!open)} className="rounded-lg p-2 text-ink lg:hidden" aria-label="Toggle navigation" aria-expanded={open}>{open ? <X /> : <Menu />}</button></div>{open && <nav className="container-page flex flex-col gap-1 border-t border-slate-200 py-4 lg:hidden" aria-label="Mobile navigation">{navigation.map((item) => <Link onClick={() => setOpen(false)} className="rounded-lg px-3 py-3 font-medium hover:bg-blue-50" key={item.href} href={item.href}>{item.name}</Link>)}<Link href="/contact" className="mt-2 rounded-lg bg-brand px-4 py-3 text-center font-semibold text-white">Get Free Consultation</Link></nav>}</header>; }
